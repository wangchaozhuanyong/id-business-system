# V2 生产闭环执行清单

本文只记录生产闭环状态和执行门禁，不扩大 `docs/V2_PRODUCT_SCOPE.md` 定义的功能范围。

## 当前生产基线

- 已部署 Git 提交：`0dd1f84b279e639eb5c2ce588f7e2e770ac84c80`
- Cloudflare Worker 版本：`d394845e-e485-43ce-b334-85fc83ecf079`
- Prisma：生产 `_prisma_migrations` 保留 79 条历史成功记录，当前纯净链 18 个 migration 已全部
  应用，无失败记录、无本地待应用记录；`20260802010000_normal_id_status_seed` 已补齐系统“正常”
  状态，`20260802011000_exchange_rate_validation_function` 已补齐汇率运行校验函数
- 数据治理：三个增量 migration 已应用，四张治理表已存在
- 财务：`2026-07-30T17:38:02.617Z` 启用，自动回填留有 4 条审计记录，但历史确认因测试说明和
  未核对自有资金账户、历史开支而重新打开；当前状态为 `incomplete`
- 财务基础资料：0 个自有资金账户、1 个供应商钱包；历史开支是否确实为 0 仍需负责人确认
- 账号：一个真实管理员和一个 `production_smoke_readonly` 自动巡检账号
- 最近部署验收：主页、live/ready 健康、只读登录/登出及订单、续费、ID、客户、开通、余额和有效汇率
  接口已通过生产 smoke

生产聚合状态必须通过只读命令重新生成，不得直接照抄本文中的数量：

```bash
npm run audit:v2-production-closure:production
```

输出只包含聚合数量、角色编码和闭环状态，不输出用户名、手机号、邮箱、Token 或业务明细。

## 2026-08-02 隔离恢复与权限复核记录

本次使用生产自定义格式备份 `pre-migration-0dd1f84-20260802T0815Z.dump` 在独立 Supabase
PostgreSQL 17.6 环境恢复。备份归档时间为 `2026-08-02T08:15:03Z`，大小为 2,065,058 字节；
恢复时保留原始 owner 和 ACL，再应用当前纯净链的两个后续 migration。最终数据库共有 79 条成功
migration 记录，当前 18 个 migration 全部已应用，无失败或待应用记录。

隔离环境已实际启动 Database、Auth、REST、Storage、Realtime、Kong 和 Studio；Auth、REST、Storage
健康端点均返回 200，Realtime 容器健康。恢复数据包含 `auth`、`storage`、`realtime`、Vault、
`pg_cron` 和 `pg_net`。本地 CLI 使用新版 Realtime 时要求 `_realtime` 工作 schema，因此只在隔离环境
补建兼容 schema；生产未做该变更。随后使用恢复库启动当前 API，live/ready、只读登录、当前用户、
订单、续费、ID、客户、开通、余额、有效汇率和登出共 12 项检查全部通过。

从第一次干净数据库就绪到完整服务与应用验证通过约 16 分 19 秒，包含首次镜像下载和错误恢复路径，
作为本次实测 RTO。备份时点到 `2026-08-02T14:17:56Z` 对比时点的观察窗口为 6 小时 2 分 53 秒：
恢复库为 2 个 ID、4 个订单和 14 张财务凭证，生产为 3 个 ID、5 个订单和 16 张财务凭证；用户、
Supabase Auth 用户、角色、权限、客户、礼品卡、开通、治理任务和 Storage 对象数量一致。上述差异
属于备份后新增数据，不能描述为恢复丢失；正式灾备的 RPO 仍由实际备份频率决定。

生产只读权限复核显示：`admin` 1 人，显式关联 10 项财务、敏感卡号、供应商资金和续费设置权限；
前后端同时存在 `admin` 权限门禁豁免，因此其有效权限覆盖全部模块。`production_smoke_readonly` 1 人，
仅有 ID、开通、余额、汇率、订单、续费和客户 7 项查看权限，本次恢复库接口验证未发现写入或敏感
资料权限。当前没有已启用的财务或普通员工角色，不得为验收创建虚假账号。

认证门禁仍未完成：生产有 1 个启用管理员，但已配置和已启用 MFA 的用户均为 0，全局 MFA 与管理员
强制策略均未开启。账号持有人完成 MFA 绑定前，不能把生产权限与认证复核标记为通过。

## 1. 财务历史闭环

执行生产写入前，由业务负责人提供并确认：

- 财务启用和历史切账日期；
- CNY、MYR、USDT 资金账户及各自期初余额；
- 加卡供应商钱包期初余额；
- 系统外历史开支、手续费、退款和损耗；
- 可核对的汇率来源、日期和证据；
- 历史资料完整性确认说明。

自动回填已经执行，后续不得在没有新预览指纹的情况下盲目重跑。剩余执行顺序固定为：只读预览、
生产备份、核对或补录期初资料与历史开支、逐源对账、负责人确认。只有预览证明仍有未处理来源时，
才冻结新预览指纹并幂等重跑回填。历史确认前，
经营分析不得将累计利润描述为完整经营利润。已经生成的财务凭证只能冲销再重记，禁止原地修改。

完成门禁：

- `history_status = completed`；
- 自动回填预览显示没有遗漏的历史来源，或重跑结果有完整审计；
- 原币、汇率、汇率来源和人民币折算证据齐全；
- 每张凭证借贷平衡并能追溯到来源业务；
- 期初账户、供应商钱包和历史开支由负责人逐项确认。

## 2. 生产权限与认证

`production_smoke_readonly` 是自动发布巡检账号，不要求创建 Supabase V2 身份，也不计入真实业务员工
缺口。新增真实员工后，必须逐账号完成本地用户、Supabase Auth 身份、角色和会话映射。

验收至少覆盖管理员、财务和普通员工三类职责；如果当前没有对应员工，记录为“角色尚未启用”，不得
创建虚假生产账号。逐项核对菜单、读写权限、敏感字段权限、会话撤销、首次改密、MFA 和 IP 白名单
防锁死门禁，并检查审计日志。

当前 22 个模块的入口矩阵：

| 访问门禁                   | 模块                                                       |
| -------------------------- | ---------------------------------------------------------- |
| 已登录用户                 | 仪表盘、我的账户                                           |
| 仅 `admin`                 | 业务监控、数据治理、员工账户、角色权限、安全中心、系统监控 |
| `apple.account.view`       | ID 管理                                                    |
| `apple.activation.view`    | 开通记录                                                   |
| `apple.balance.view`       | ID 加额、加卡记录、ID 报损记录                             |
| `apple.exchange_rate.view` | 汇率记录                                                   |
| `apple.order.create`       | 订单录入                                                   |
| `apple.order.view`         | 订单管理                                                   |
| `apple.renewal_task.view`  | 续费操作                                                   |
| `audit_log.view`           | 审计日志                                                   |
| `customer.view`            | 客户记录                                                   |
| `data.analytics.view`      | 经营分析                                                   |
| `data.dictionary.manage`   | 选项设置                                                   |
| `finance.view`             | 财务记账                                                   |

上述只是页面入口门禁，写入、敏感资料和调整类权限仍需按真实职责逐项选择，不能只凭“页面能打开”
判定验收通过。

## 3. 真实业务闭环

使用有明确测试标记且可冲销的数据，依次验收供应商付款、礼品卡加卡、ID 入账、订单匹配、订单完成、
手工续费和退款。核对供应商余额、ID 余额、开通记录、成本、利润、不可变财务凭证和审计日志。

测试完成后通过退款、撤销或冲销恢复，不直接删除财务记录。

## 4. 数据治理双人演练

第一轮只恢复专门创建并软删除的测试客户或测试订单，不执行真实历史硬清理：

1. 管理员 A 创建确定性预览并填写备份证据；
2. 验证管理员 A 不能审批自己的任务；
3. 管理员 B 核对预览哈希并批准；
4. 授权执行人分批执行；
5. 重放同一幂等键，确认不会重复处理；
6. 核对任务、审批、检查点、逐项结果和审计编号。

2026-08-12 已完成第一轮生产演练：管理员 A 为专用测试客户创建任务
`GOV-20260812-9dfc8634`，独立生产巡检身份临时获得管理员角色后核对预览哈希并审批；执行成功
`1/1`，相同幂等键重放返回同一检查点且没有重复处理。演练暴露并修复了“角色变化不撤销旧会话”
问题，提交 `5736860` 已全量发布；发布后分别验证角色升级和降级均使旧令牌立即返回 `401`。巡检身份
最终恢复唯一 `production_smoke_readonly` 角色和 `0` 在线会话；未登录的临时账号保持停用并移除
管理员角色。生产完整性审计 30 项无违规，闭环审计结果为 `ready: true`。

## 5. 托管数据库恢复

正式演练必须使用兼容 Supabase Auth、Vault、`pg_cron` 和 `pg_net` 的隔离环境。恢复后比较备份时点
行数和备份后新增数据，并验证登录、权限、订单、客户、ID、财务和数据治理接口。记录恢复耗时、
数据缺口、RPO、RTO 和操作手册。普通 PostgreSQL 的业务表恢复不能替代完整 Supabase 恢复证据。
