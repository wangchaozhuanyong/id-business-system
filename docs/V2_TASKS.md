# ID 业务管理系统任务清单

每次只执行一个明确任务。完成后记录代码、接口、数据库、检查和未完成事项。

## 当前执行

- [ ] 数据删除与恢复一致性加固：收紧生产运行账号的物理删除权限；修复选项级联软删除唯一键与供应商
      钱包恢复状态；为客户和选项删除增加确定性依赖预览、执行前复核和活动业务保护；冻结订单与开通记录
      的历史展示名称；新增可重复的只读数据一致性巡检，并完成迁移、自动化测试和生产验收。

## 当前已完成

- [x] 全站浅色主题内容层级统一：顶部栏改为浅色表面，页面概览模块统一为浅冷灰外层、白色指标层、
      深色正文和蓝灰强调；登录页与品牌预览同步跟随深浅主题，侧栏保持原有深色表现。新增深浅主题
      概览、登录画布、桌面与 390px 对比度和布局验收。本任务未新增接口、数据库字段或 migration。
- [x] 全站深色主题组件统一：接入 Element Plus 官方深色协议，建立不受按需样式加载顺序影响的
      全局主题令牌桥接，统一表单、表格、分页、状态、浮层、弹窗和抽屉的深浅主题；新增桌面与窄屏
      真实组件验收。本任务未新增接口、数据库字段或 migration。
- [x] 菲律宾国家货币自动匹配：国家选项新增菲律宾比索 `PHP`，新增国家输入“菲律宾”或英文名称时
      自动带出 `PHP`，手动选择的货币不被覆盖；关联业务继续继承国家默认货币。本任务未新增接口、数据库字段或 migration。
- [x] ID 供应商下拉完整展示：ID 新增和编辑时同时展示启用、停用供应商，停用项明确标注且不可选择；
      选择器接口新增 `includeDisabled` 查询参数并返回状态。本任务未新增数据库字段或 migration。
- [x] 订单录入“业务与对象”字段位置调整：国家移动到原业务分类左侧位置，业务分类移动到原国家右侧位置；
      保持字段绑定、必填校验、国家与业务联动不变。本任务未新增接口、数据库字段或 migration。
- [x] ID 报损记录并入 ID 管理“已报损”分类：切换分类后在当前页面直接展示报损筛选、列表与恢复操作，
      不再跳转独立页面；取消左侧独立导航入口，保留原路由兼容已有书签。本任务未新增接口、数据库字段或 migration。
- [x] ID 生命周期与安全操作：ID 管理默认只显示可用 ID，提供可用、已停用、已售出和已报损快捷分类；
      停用/启用必须填写原因并可查看，已售出 ID 禁止停用和报损；
      报损记录支持受控恢复并永久保留冲回快照；取消 ID 删除入口与接口，保留既有历史软删除数据
      和回收站恢复能力。新增 migration `20260808120000_account_lifecycle_management`，本地迁移、
      全量类型、测试、构建、桌面与窄屏浏览器验收均已完成。
- [x] ID 管理入口与页面整理：统一将“ID录入”改为“ID管理”，重新划分页面操作、常用筛选、
      高级筛选、安全提示和数据列表层级，保持现有接口、状态规则和权限行为不变。
      桌面及移动端 8 个视口无页面溢出、筛选溢出或按钮裁切；本任务未新增接口、数据库字段或 migration。
- [x] 订单录入三模块重构：业务与对象、客户与结算、周期与备注按指定左右字段分组；
      ID 选择独立为两至三列并支持每页 12 条分页，实时核算独立显示，已保存订单与左侧主表单对齐。
      本任务未新增接口、数据库字段或 migration。
- [x] 订单与加卡记录表格操作区整理：订单右侧固定必显“状态/下一步”列并收纳扣减、确认开通，
      通用操作列仅保留详情、修改和更多；加卡记录文字按钮完整显示；专项桌面和移动视口无横向溢出。
      本任务未新增接口、数据库字段或 migration。
- [x] 全局数据表列显示设置：41 张注册表统一提供列设置抽屉，偏好按用户和表标识存入数据库，
      桌面和移动卡片同步生效；支持搜索、全部显示、恢复默认、关闭保护与审计，并通过刷新持久化浏览器验收。
- [x] 礼品卡入账界面重构：合并重复计算信息，改为目标 ID 概览、卡片资料、结算与备注双栏结构，
      保留原有校验、卡商余额预警、确认和关闭保护，并保证窄屏无重叠。
- [x] 新增 ID 采购入账字段整理：统一“ID采购币种、ID采购金额”命名，将人民币成本移动到采购金额下方，
      并明确标记为系统自动计算、无需手动填写。
- [x] 跨页面数据新鲜度与多人并发一致性：设置新增项通过共享失效缓存自动进入订单快捷录入；
      账户余额与候选 ID 接收后台刷新结果；Realtime 异常时每 15 秒版本补偿并在页面回到前台时校验；
      服务端行锁、串行化事务、冲突重试和幂等账本通过专项测试。
- [x] 敏感资料角色策略与管理员审核闭环：管理员直接查看；普通角色可配置直接查看或申请审批；
      申请、批准、拒绝、15 分钟授权和实际查看均写审计，并通过 V2 通知入口提示管理员。
- [x] 单一管理端与 API 入口
- [x] 登录、角色权限与审计
- [x] Supabase 稳定会话、首次强制改密、全设备会话撤销与前端刷新闭环
- [x] ID 与客户资料
- [x] 业务选项设置
- [x] 加卡、移动加权平均成本与不可变余额流水
- [x] 订单录入、自动匹配、锁定、余额扣减与利润
- [x] 订单录入自动/手动合格 ID 选择与预计利润反推收款建议
- [x] 订单查询、修改、退款、取消与软删除
- [x] 开通记录
- [x] 手工续费闭环
- [x] 续费日期筛选、1–365 天预警设置与顶部提醒
- [x] 汇率自动采集、人工记录与设置
- [x] 订单录入右侧快捷新增客户并自动回填
- [x] 订单录入稳定两列布局、目标利润与实时成本核算
- [x] ID 保留/卖出/收回、成本快照、建议价格、订单与 ID 管理闭环
- [x] 已卖出 ID 在匹配、加卡、余额调整和续费入口统一排除
- [x] ID 报损冻结、余额成本保留、损耗入账、解除冻结冲回与 ID 报损记录
- [x] 加卡记录“被赎回”可原子联动报损冻结 ID
- [x] 加卡供应商期初资金、USDT 付款、加卡扣减、撤回返还、调账与付款撤销闭环
- [x] 加卡记录国家/供应商快照、供应商余额快照、付款报表与完整卡号直接展示
- [x] 员工账号开通、角色分配、启停、会话撤销与变更审计
- [x] 角色创建、权限矩阵、角色成员查看、系统角色保护与变更审计
- [x] 审计日志筛选、变更详情、敏感访问查看、敏感字段双层脱敏与导出留痕
- [x] 安全中心登录风险、在线会话下线、MFA 绑定与管理、IP 白名单和防锁死门禁
- [x] 我的账户脱敏资料、修改密码入口、MFA 和当前账号在线设备自助管理
- [x] 权限感知的经营仪表盘：今日业务、风险待办、ID 库存账面成本和审计摘要
- [x] 管理员业务监控：九条源状态规则、服务端分页筛选、异常汇总和源数据处理入口
- [x] 管理员系统监控：API、数据库、版本补偿、汇率任务、认证聚合和不可观测项声明
- [x] 数据治理只读预检 API：软删除盘点、既有导入导出能力、保留清理与备份证据缺口
- [x] 数据治理闭环：回收站恢复、汇率历史受控清理、确定性预览、异人审批、分批检查点和逐项审计
- [x] 数据治理审批就绪门禁：核验其他启用管理员，前后端阻止创建无人可审批的治理预览
- [x] 数据治理待审批任务取消闭环：仅申请人可取消，保留不可变预览并写入事务审计
- [x] 表单左侧标签与必填星号右置规则
- [x] 全站业务表单可提交校验、错误计数、首错定位与硬阻塞原因
- [x] 应用壳、查询缓存、共享骨架、意图预取与 Chunk 恢复
- [x] 区域查询受控阶段、参数切换旧快照只读、稳定分页与详情迟到响应隔离
- [x] 数据变化驱动缓存、私有 Realtime scope 失效、版本补偿与时间 deadline
- [x] 桌面端、平板和移动端基础验收
- [x] 全站数据表语义固定列、弹性文本列与容器自适应列间距
- [x] 浅色与暗色主题前景/背景对比度、公共按钮全状态和自动质量门禁
- [x] 管理平台完整骨架：仪表盘、数据中心、监控中心和系统管理入口
- [x] 删除未注册页面、API、脚本、文档和运行入口
- [x] 生产发布预检、Git 提交溯源、主分支保护与最小权限自动巡检
- [x] 多币种财务盈亏闭环：账户、卡商钱包、开支、自动记账、经营分析、资产估值、对账和关账

## 可靠性重构验收

- [x] 金额领域统一为 `Amount4` / `Rate8`，数据库 Decimal 只在 persistence row mapper 出现
- [x] 写命令统一由 `V2CommandTransactionManager` 管理事务、幂等重放、财务过账和同步审计
- [x] ID 报损拆分为 command handler、repository、posting coordinator 和 query service，并复用外层事务
- [x] 会话统一由 `SessionCoordinator` 管理原子凭据、身份代际、跨标签验证、熔断和只读降级
- [x] API 错误统一输出稳定错误码、request ID、重试语义和向后兼容的 `message`
- [x] 生产刷新性能：API 固定到数据库同区、会话活跃时间限频写入、选择项批量读取和分阶段 Server-Timing
- [x] AST 动态盘点的全部真实桌面表迁移到显式 `V2Table + typed schema`，删除隐式表格代理和双轨列算法
- [x] Prisma runtime、Decimal、直接模型访问、直接事务、raw SQL 和 persistence 外 row mapper 纳入静态门禁

本次可靠性重构不新增数据库表、字段或 migration，原有路由、DTO、幂等键和金额字符串响应保持兼容。
表格数量由 `check:v2-table-standard` 每次从模板和 schema 动态计算，不在任务文档中固定数量。
冷启动与已验证会话 503、Node/Cloudflare Decimal adapter、表格多视口稳定性分别由专用 acceptance
命令验收；这些本地结果不代表已执行生产报损、部署或线上迁移。

ID 销售闭环使用增量 migration
`20260729070000_order_account_disposition`，历史订单默认 `retained`，不回算历史利润。
订单保留 ID 购买成本快照，并通过 `appliedAccountCostAmount` 明确本单实际计入值。退款默认保留
销售占用，只有明确提交 `accountReturned=true` 才恢复 ID 可用并重算利润。

ID 报损先由增量 migration `20260729085000_account_loss_ledger_type` 单独提交流水枚举值，再由
`20260729090000_account_loss` 创建基础约束和报损结构，避免 PostgreSQL 在同一事务中拒绝引用新枚举值。
`20260806100000_account_loss_freeze_reversal` 将报损升级为 active/reversed 生命周期：报损冻结时保留
ID 余额和人民币成本、写入损耗财务日记并关联当前 active loss；解除冻结必须先生成反向财务日记冲回
损耗，再恢复 ID 状态并清除 active loss 指针。已卖出 ID 禁止新增报损，历史普通冻结状态不自动回填
为报损，解除冻结不自动恢复历史开通记录，原报损记录保留为已冲回状态。
礼品卡联动报损复用该表和流水类型；卡片损失与扣卡后剩余 ID 损失分别记账，并使用同一原因和确定性
幂等关联。

生产发布加固任务新增固定 Cloudflare 目标、干净且已同步的 `main`、必需 CI 和分支保护预检；
Wrangler 版本写入 Git commit，部署后自动使用 `production_smoke_readonly` 独立账号检查公共健康端点、
最小权限集合和核心只读接口。只同步所需查看权限并新增角色和账号数据，不新增表、字段或 Prisma
migration。

Supabase 认证闭环复用现有 `V2AuthIdentity.mustResetPassword`、`lastAuthenticatedAt` 和
`ActiveSession.tokenHash`，不新增表、字段或 migration。Provider session 只在登录成功时显式登记，
历史未登记会话不会在请求阶段自动补建；切换到该版本后，已有 Supabase 登录用户需要重新登录一次。

员工账号和角色权限代码及生产发布已经完成。角色编码创建后保持不可变，`admin` 系统角色只读，当前
不提供角色硬删除或启停；生产当前只有真实管理员和 `production_smoke_readonly` 自动巡检角色，新增
真实员工后仍需按职责逐账号复核 Supabase 身份、角色权限和会话撤销。多币种财务 migration 已在生产
应用，但历史状态仍为 `incomplete`，历史回填、业务期初数据和遗漏开支确认仍属于生产闭环待办。

数据治理闭环使用三个增量 migration：`20260731010000_data_governance_workflow` 建立任务、条目、
审批和检查点结构，`20260731011000_data_governance_updated_at_defaults` 补齐数据库更新时间默认值，
`20260731012000_data_governance_preview_guards` 固化预览哈希、异人审批和执行范围不可变约束。三个
migration 已在本地开发库和生产环境应用。2026-08-12 已使用两个独立生产身份完成专用测试客户的
恢复预览、异人审批、单条执行和同键幂等重放；任务 `GOV-20260812-9dfc8634` 状态为 `succeeded`，
第二次执行返回同一检查点且标记为重放。演练后巡检身份已恢复唯一只读角色并撤销全部临时会话。
治理概况现在按当前申请人返回启用管理员总数、可作为异人审批人的管理员数量和阻塞原因；创建恢复或
清理预览会在业务预检前及创建事务内重复核验，已有任务的同键幂等重放不受后续管理员状态变化影响。
本次审批就绪门禁不新增表、字段、权限或 migration，也不会自动创建生产管理员。
本地隔离验收会从空 PostgreSQL 16 数据库应用全部 19 个 migration，再由两个独立测试管理员完成
恢复预览、禁止自批、异人审批、预览不可变、分批执行和幂等重放；命令为
`npm run acceptance:v2-data-governance`。本地备份恢复演练还需与托管数据库的真实备份链路分开记录。

## 当前待办

- [ ] 复核已执行的多币种财务历史回填，并由业务负责人确认期初余额与遗漏开支
- [ ] 按真实生产账号复核当前 22 个真实模块的权限矩阵（当前管理员与只读巡检角色已完成数据库侧
      权限核对；管理员 MFA、真实菜单/写入/敏感字段交互仍待账号持有人验收）
- [ ] 使用真实业务数据完成一次加卡、订单、退款和手工续费闭环
- [ ] 对新增真实员工逐账号复核 Supabase 身份、强制改密标记并验证会话撤销
- [x] 在 Supabase 兼容隔离环境完成全量恢复和备份后新数据对比
- [x] 使用两个独立生产身份完成数据治理预览、异人审批、分批执行和幂等重放演练
- [ ] 将生产 API 从 Cloudflare Free Worker 迁移到 Supabase Edge Function，并完成财务写入验收

生产闭环步骤、负责人输入和验收门禁见 `docs/V2_PRODUCTION_CLOSURE.md`。只读聚合审计命令为
`npm run audit:v2-production-closure:production`。

当前共有 22 个注册模块，均为真实模块。数据治理恢复接口只覆盖 ID 资料、客户、业务选项和订单的
软删除记录；清理只覆盖超过保留期且未被礼品卡引用的汇率采集历史，通用硬删除保持关闭。产品边界
与数据规则见 `docs/V2_PRODUCT_SCOPE.md` 和 `docs/V2_ARCHITECTURE.md`。

## 每个任务的完成要求

1. 只修改任务直接相关文件。
2. service 提供基础单元测试。
3. 数据库修改使用 Prisma migration。
4. 前端覆盖加载、错误、空状态、权限和移动端。
5. 运行相关 typecheck、test、lint、build 和架构检查。
6. 记录新增接口、表、字段、迁移命令与剩余风险。
