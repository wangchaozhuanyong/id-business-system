# ID 业务管理系统任务清单

每次只执行一个明确任务。完成后记录代码、接口、数据库、检查和未完成事项。

## 当前执行

- [x] 生产 MySQL 性能观测与维护窗口探针：每 5 分钟只读汇总连接池、异常断连增量和最近慢查询
      digest，不记录 SQL 参数；发布安装器验证首次巡检并在失败回切时恢复上一版 timer。新增只读生产
      渐进压测与 SSE soak 脚本，限制并发不超过 40，遇到错误、p99 或单请求耗时超阈值立即停止。
      本任务新增 3 个可选生产观测阈值，不新增依赖、接口、数据库表、字段或 migration。

- [x] 事件驱动实时数据更新：业务事务成功提交后通过本地 Node API 的 JWT SSE 通道仅推送精确
      scope/version；连接正常时停止固定版本轮询，没有新数据不刷新业务列表；25 秒心跳只维持连接，
      断线自动重连并临时启用 15 秒版本补偿，恢复联网和页面回到前台时立即核对。本任务不新增依赖、
      环境变量、数据库表、字段或 migration，也不恢复 Supabase/Cloudflare 运行时。

- [x] 生产发布制品与镜像保留闭环：每次发布上传前、切换成功后及每日定时执行同一受锁清理脚本；
      发布目录保留最近 5 份、不可变制品保留最近 3 份，并始终额外保护当前和上一已验证提交；
      Docker 只移除本项目过期镜像标签及无容器引用的悬空层，禁止清理数据库卷、备份或执行
      `docker system prune`；发布上传前和镜像加载前均要求至少 8 GiB 可用空间。本任务不新增接口、
      数据库表、字段、migration 或生产环境变量。

- [x] 生产不可变制品发布闭环：正式标签 CI 只构建一次 API、管理端、migration 和发布
      门禁镜像，生成带完整 commit、CI/部署运行号、制品 SHA-256、四个镜像 digest、
      操作人和上一生产 SHA 的发布清单；AWS 安装器持锁加载同一制品，串联 S3 备份、
      向前 migration、数据库账号门禁、38 项财务完整性、登录与核心只读巡检、越权拒绝、
      登出失效、原子切换和失败回切，生产服务器禁止重新构建。本任务不新增业务接口、
      数据库表、字段或 migration。

- [x] 邮箱查询码可用性与 Microsoft 批量授权：修正双列表单标签和输入框对齐；完整显示可恢复查询码并提供
      一键复制，历史不可恢复查询码提供“生成并复制”；新增 1 至 365 天全局有效期设置、现有码同步选项、
      72 小时小时/分钟倒计时和到期标红，固定使用人工换码避免静默失效；批量导入增加 Microsoft 并复用
      同一授权窗口逐个完成 OAuth2。新增双数据库增量 migration 和设置修改审计。
- [x] 邮箱查询零内容留存：保留完整邮箱和完整查询码显示，邮件内容仅实时经过
      API 内存；公开查询限流和 Microsoft OAuth 临时状态改为有界进程内存，不再写入
      MySQL/PostgreSQL；增量 migration 移除两张历史临时表，所有相关响应禁止浏览器持久缓存。

- [x] 邮箱池紧凑表格与 Microsoft 查询：添加邮箱表单改为双列紧凑布局，邮箱池改为每行一个邮箱的
      统一数据表并提供常驻分页、完整查询码快捷复制和右侧批量导入；新增 Microsoft OAuth2 + IMAP
      查询、刷新令牌与查询码字段级加密、OAuth 状态表及双数据库增量 migration。历史只存 HMAC 的
      查询码保留兼容，重置后才可显示完整值。

- [x] 邮箱 IMAP 连接韧性与批量账号保护：按官方顺序尝试 iCloud 登录名，扩展授权错误识别，管理员
      验证授权遇到瞬时连接失败时执行一次短延迟重试；每家邮箱服务限制为 4 个并发连接和 24 个排队请求，管理端显示
      Gmail/iCloud 应用专用密码指引，并按请求编号记录不含邮箱或密码的失败类别。本任务不新增依赖、
      环境变量、数据库字段或 migration。

- [x] 邮箱查询码单字段闭环：买家公开页改为只输入邮件查询码，后端通过唯一查询码 HMAC 定位邮箱，
      查询尝试按查询码和来源 IP 限流，并兼容历史 `邮箱----邮件查询码` 请求；管理端创建和重置后只
      返回查询码。本任务新增查询码哈希唯一索引，并将查询尝试限流键由邮箱哈希迁移为查询码哈希。

- [x] AWS Node 邮箱入口恢复：在当前 AWS/MySQL Node/Docker 运行链路注册受管邮箱池和公开查询接口，
      恢复个人工作区邮箱抽屉与公开 `/mailbox` 页面；公开查询绕过前端登录会话门禁，但邮箱池管理继续
      仅限管理员，应用专用密码继续加密保存，邮件正文仅按需通过 IMAP 读取且不持久化。本任务不新增
      依赖、环境变量、数据库表、字段或 migration，也不接入第三方代查服务。

- [x] 生产代理链真实客户端 IP 闭环：由 Caddy 覆盖外部转发头，Nginx 仅转发单一可信值，API 只信任
      紧邻的一跳；移除未在生产入口落地的自定义签名头方案，补齐代理配置契约、客户端 IP 解析和环境
      隔离测试，确保登录限流、IP 白名单、公开邮箱限流及审计使用真实客户端 IP。本任务不新增接口、
      数据库字段或 migration。

- [x] 生产数据库身份与最小权限闭环：迁移、API 运行、只读审计和备份使用四个独立 MySQL 身份；
      运行账号禁止 DDL 和全库 DELETE，仅保留受控表物理删除权限；新增可重复账号供应、SHOW GRANTS
      发布门禁和临时 MySQL 权限验收，更新 Compose、备份脚本与部署文档。本任务不新增业务表、字段或
      migration。

- [x] CSV 公式注入闭环：统一中和导出字符串中的公式前缀、控制字符和前导空白绕过，同时保持数值列、
      CSV 引号转义及 ID 资料导出后再导入兼容；覆盖 ID 资料、审计日志和通用导出工具专项测试。本任务
      不新增接口、数据库字段或 migration。

- [x] 公共邮箱查询码有效期闭环：新增查询码到期时间，新建和重置后默认 30 天有效，现有查询码在
      migration 时获得 30 天过渡期；公开查询对无效和过期凭据使用同一响应，邮箱池展示到期状态并
      保留重置审计。本任务新增 `IdBusinessV2ManagedMailbox.queryCodeExpiresAt` 和对应增量 migration，
      不新增表或公开接口。

- [x] 生产容器与本机配置边界加固：API 运行镜像只保留生产依赖和构建产物，以非 root、只读根文件
      系统、无额外 Linux capabilities 运行；迁移镜像继续保留 Prisma CLI；固定 Node、MySQL、Nginx、
      Caddy 镜像 digest，扩展 AWS-only 环境文件门禁并清理本机遗留配置。本任务不新增接口、数据库字段
      或 migration。

- [x] 财务与余额计算完整性加固：将生产数据审计迁移为 MySQL 8 全量只读快照检查并扩展到 38 项，
      新增固定只读审计账号的权限收敛和发布阻断门禁；经营分析对账由每类前 50 条抽样改为游标分批
      全量扫描、精确问题计数和有界明细响应；新增真实 MySQL 入账、故障回滚、幂等、并发冲销验收，
      以及 32,768 步独立 BigInt 状态模型复算。新增 migration
      `20260828120000_financial_integrity_audit_mysql`，只增加安全触发器存在性函数，未新增业务表、字段或接口。

- [x] AWS MySQL 生产备份加固：移除仍调用已删除 PostgreSQL 脚本的失效 GitHub 定时任务；
      EC2 改为每 30 分钟生成逻辑备份，强制上传私有 S3 并校验大小与 SHA-256，本机使用
      48 份和 1 GiB 双上限轮换，S3 保留 90 天；新增每周从 S3 下载最新备份、恢复到无宿主机端口的临时 MySQL 8.4
      容器并检查核心表、Prisma migration 与表完整性的 systemd 演练。2026-08-28 已部署生产并完成
      首次真实 S3 恢复：63 张表全部通过 `CHECK TABLE`，10 个 migration 有效且临时容器清理完成。
      本任务未新增接口、表、字段或 migration。

- [x] 手工资金流入闭环：将“开支记账”升级为“收支记账”，提供“收入记账”和“开支记账”双入口；
      新增可维护收入分类，以及受控的经营收入、股东投入和借入资金三类资金性质。入账同步锁定原币、
      汇率和收款账户快照，在同一事务生成不可变总账、更新账户余额并写入审计；更正只允许冲销并重记。
      经营分析分别展示其他经营收入、股东投入和借入资金，后二者不得进入经营利润；收款流水号和收款
      凭证允许留空，填写或上传时继续执行防重、加密和完整性校验。

- [x] 安全审查修复：统一 MFA 强制语义和会话证明，修复 MFA 一次性凭据并发重放、
      登录失败与公开邮箱查询非原子限流、可信代理链客户端 IP 校验、管理端 CSP 和开发工具链
      高危依赖，并将浏览器 bearer 凭据迁移到标签页级存储；补齐专项测试与全量质量门禁。
      `User.phone` 已改为密文与脱敏快照存储，新增 migration `20260815120000_encrypt_user_phone`、可重复历史
      回填和禁止明文约束，并纳入受备份门禁保护的生产 migration 固定流程。本地全量质量门禁和一次性
      PostgreSQL 历史数据演练已通过；生产状态以固定 migration、发布和巡检的实际结果为准。

## 当前已完成

- [x] 多币种人民币收购价完整闭环：初始化 15 个独立币种配置，使用 BigInt 十进制算法按各币种兑人民币
      市场汇率、独立比例、显示单位和舍入方式生成不可变快照；接入无需 API Key 的 ExchangeRate-API
      免费 CNY 基准开放接口、北京时间每天 09:05 数据库 Cron、唯一运行锁、有限重试、全币种原子发布、失败保留旧值、过期提醒和
      10% 默认异常审核。后台支持手动刷新、采集/快照历史、确认或驳回异常批次、批量比例、手工汇率覆盖，
      以及微信/等宽/纯文本生成和一键复制。新增 migration `20260820090000_purchase_quote_calculation`、
      `20260820095000_purchase_rate_provider_enum`、`20260820100000_purchase_rate_automation`、
      `20260821113000_purchase_rate_open_provider_enum` 和 `20260821114000_purchase_rate_daily_open_provider`；
      任何外部消息软件群发仍明确不在范围。

- [x] 邮箱查询入口占位：在个人工作区的在线工具中恢复“邮箱查询与邮箱池”禁用入口，
      明确标记邮件服务部署后启用；本任务不挂载邮箱抽屉，不注册公开页面、邮箱接口或 Node IMAP 运行时。

- [x] 登录页未登录 2FA 工具：在动态验证码输入项下方新增公开入口，复用现有右侧
      TOTP 抽屉和纯前端计算逻辑；未登录时不请求受保护的服务器时间接口，使用本机时间并
      显式提示可能存在偏差。本任务未新增接口或数据库变更，随本次前端发布上线。

- [x] 暂停邮箱查询生产入口：在 Node IMAP 运行时尚未部署期间，工作区不展示邮件查看器，不注册公开
      `/mailbox` 页面、公开邮箱查询接口或邮箱池管理接口；保留已验收源码供后续恢复。本次发布继续交付
      个人快捷网址与浏览器本地 2FA 工具。

- [x] 自有 Gmail/iCloud 邮箱查询：移除第三方代查接口，新增受管邮箱池、加密应用专用密码、
      HMAC 买家查询码、查询尝试限流日志和公开 `/mailbox` 查询页；工作区右侧抽屉支持邮箱分页搜索、
      停用、更新授权和重置查询码。新增 migration `20260815100000_managed_mailboxes`，邮件正文只按需
      从 Node IMAP 运行时读取并转换为纯文本，不持久化邮件内容。本任务未执行生产迁移或部署。

- [x] 个人工作区与在线 2FA 工具：全局侧栏左下角新增当前用户工作区，支持按用户保存、
      编辑、删除和排序最多 20 个快捷网址，点击后在浏览器新标签页打开；新增右侧抽屉内的
      2FA 工具，支持 Base32、TOTP URI 和多行账号文本，只在浏览器内存中计算并在关闭时清空。
      新增 migration `20260814130000_user_workspace_shortcuts` 和快捷网址 CRUD/排序接口；空库全迁移、全量质量门禁、
      桌面与 390px 窄屏浏览器验收均通过。本任务仅完成本地开发与验收，未执行生产迁移或部署。
- [x] 个人 2FA 账号加密保存：保留未登录与临时纯前端查询，已登录用户可在同一抽屉内添加、
      搜索、编辑换绑和删除最多 100 个个人账号；密钥使用字段级加密与 HMAC 去重，接口不回传密钥、密文或
      HMAC，验证码由服务器生成并按到期时间自动刷新，增删改写审计且不记录敏感值。新增 migration
      `20260828170000_user_totp_accounts`；生产迁移与部署按标准发布流程执行并绑定合并后的 `main` 精确 SHA。
- [x] 加卡记录不可操作原因显式展示：已报损 ID 保留供应商更正，冻结备注和余额操作，并在桌面、
      移动端统一显示“ID 已报损冻结”；无任何可用操作时显示权限原因。

- [x] 全站敏感资料展示策略重构：管理员业务资料默认完整显示，密码和密保仅受控点击查看，审计始终
      脱敏；员工角色可按字段和使用位置配置不显示、仅脱敏、点击查看、审批查看或完整显示，且不能
      绕过基础权限。账号、客户联系方式、订单网站账号和礼品卡号使用 HMAC 盲索引保持脱敏搜索能力，
      客户微信和 QQ 改为加密存储。新增展示策略表、两组增量 migration、历史回填命令和
      `docs/V2_SENSITIVE_DATA_POLICY.md`。
- [x] 订单脱敏账号搜索与筛选区整理：ID 账号和订单网站账号新增 HMAC 盲索引，
      支持脱敏展示下连续 2 个字符以上的片段候选查询，并在服务端解密后二次校验，
      不向前端返回明文或搜索令牌；
      新增 migration `20260814020000_sensitive_search_blind_indexes` 和可重复执行的历史数据回填命令。
      订单筛选改为搜索与常用筛选分行布局，增加条件计数和一键重置，完成桌面、窄屏、
      首页、尾页与空状态验收。
- [x] 已售出 ID 余额成本所有权闭环：ID 明细继续保留余额与成本，订单完成售出后将剩余余额成本转为
      客户资产备查，不再计入公司 ID 库存；客户名下后续消费不重复结转，公司退款或纠正售出时按
      当时实际剩余成本恢复公司资产。同步覆盖加卡、调账、报损、经营资产报表和历史数据修正；新增
      migration `20260813090000_customer_owned_balance_cost_account` 和
      `20260813091000_sold_account_balance_ownership`，纯净库全迁移与代表性历史数据回填演练均通过。
- [x] 全系统北京时间治理：展示、时间表单、日期筛选、导出、订单编号、财务业务日和监控口径统一
      使用 `Asia/Shanghai`；新增服务器权威时间接口、前端单调业务时钟和静态检查，浏览器时区与本机
      时钟不再改变业务语义。数据库继续保存绝对时刻；新增 migration
      `20260812180000_beijing_business_timezone` 更新财务时区默认值及现有旧配置。
- [x] 加卡工作台按未售出与已售出 ID 拆分独立列表和列偏好，两个列表分别保留服务端分页与排序；
      已售 ID 加卡前明确提示销售订单和客户归属，提交时由后端在事务锁内复核售出确认，防止列表状态
      变化绕过提示。本任务未新增数据库表、字段或 migration。
- [x] 数据删除与恢复一致性加固：生产运行账号的物理删除权限已收口；选项级联软删除唯一键、供应商
      钱包恢复状态、客户与选项删除依赖预览及执行前复核均已加固；订单、开通与经营开支历史展示名称
      使用不可变快照；新增 migration `20260811100000_data_lifecycle_integrity_hardening` 和可重复只读
      一致性巡检。生产巡检 30 项无违规且已完成双身份恢复演练；本地空库全迁移、两轮巡检及治理工作流
      验收通过。验收脚本改为实际连接目标数据库后再继续，消除容器初始化期间的偶发竞态。
- [x] 已售 ID 售后业务闭环：将“已售”定义为客户归属状态，允许原客户继续下单、续费、加卡、
      调账、停用和报损；新增 `accountSource`、`sourceSoldOrderId` 和
      `appliedAccountCostAmount`，保证售后 ID 成本固定为 0；新增已售 ID 回收预检、独立售后财务报表、
      归属与重复成本完整性规则。数据库使用 migration
      `20260812100000_sold_account_after_sales`，本任务与数据删除一致性加固任务独立。
- [x] 全站中文界面治理：列设置不再展示或搜索内部 schema key，全站概览英文装饰标题与裸露时区标识
      改为中文；新增模板、脚本文案和内部字段直出检查并接入本地质量门禁与 CI，防止新功能再次引入同类问题。
      本任务未新增接口、数据库字段或 migration。
- [x] 已售出 ID 受控恢复可用：ID 管理的已售出列表新增“恢复可用”操作和必填原因；
      后端原子解除售出关联、释放占用、同步订单 ID 去向与利润，已完成入账订单自动冲回
      ID 成本并恢复库存资产，全程写入审计。新增预检查接口
      `GET /id-business-v2/orders/:id/recover-sold-account-preview` 和执行接口
      `POST /id-business-v2/orders/:id/recover-sold-account`；恢复命令本身未新增表、字段或 migration，
      当前工作区的售后 ID 来源字段由 `20260812100000_sold_account_after_sales` migration 提供。
- [x] 全站浅色主题内容层级统一：顶部栏改为浅色表面，页面概览模块统一为浅冷灰外层、白色指标层、
      深色正文和蓝灰强调；登录页与品牌预览同步跟随深浅主题，侧栏保持原有深色表现。新增深浅主题
      概览、登录画布、桌面与 390px 对比度和布局验收。本任务未新增接口、数据库字段或 migration。
- [x] 全站深色主题组件统一：接入 Element Plus 官方深色协议，建立不受按需样式加载顺序影响的
      全局主题令牌桥接，统一表单、表格、分页、状态、浮层、弹窗和抽屉的深浅主题；新增桌面与窄屏
      真实组件验收。本任务未新增接口、数据库字段或 migration。
- [x] 菲律宾国家货币自动匹配：国家选项新增菲律宾比索 `PHP`，新增国家输入“菲律宾”或英文名称时
      自动带出 `PHP`，手动选择的货币不被覆盖；关联业务继续继承国家默认货币。本任务未新增接口、数据库字段或 migration。
- [x] ID 供应商下拉完整展示：ID 新增和编辑时同时展示启用、停用供应商，停用项明确标注且不可选择；
      选择器接口新增 `includeDisabled` 查询参数并返回状态。本任务未新增数据库字段或 migration。
- [x] 订单录入“业务与对象”字段位置调整：国家移动到原业务分类左侧位置，业务分类移动到原国家右侧位置；
      保持字段绑定、必填校验、国家与业务联动不变。本任务未新增接口、数据库字段或 migration。
- [x] ID 报损记录并入 ID 管理“已报损”分类：切换分类后在当前页面直接展示报损筛选、列表与恢复操作，
      不再跳转独立页面；取消左侧独立导航入口，保留原路由兼容已有书签。本任务未新增接口、数据库字段或 migration。
- [x] ID 生命周期与安全操作：ID 管理默认只显示可用 ID，提供可用、已停用、已售出和已报损快捷分类；
      停用/启用必须填写原因并可查看，已售出 ID 禁止停用和报损；
      报损记录支持受控恢复并永久保留冲回快照；取消 ID 删除入口与接口，保留既有历史软删除数据
      和回收站恢复能力。新增 migration `20260808120000_account_lifecycle_management`，本地迁移、
      全量类型、测试、构建、桌面与窄屏浏览器验收均已完成。
- [x] ID 管理入口与页面整理：统一将“ID录入”改为“ID管理”，重新划分页面操作、常用筛选、
      高级筛选、安全提示和数据列表层级，保持现有接口、状态规则和权限行为不变。
      桌面及移动端 8 个视口无页面溢出、筛选溢出或按钮裁切；本任务未新增接口、数据库字段或 migration。
- [x] 订单录入三模块重构：业务与对象、客户与结算、周期与备注按指定左右字段分组；
      ID 选择独立为两至三列并支持每页 12 条分页，实时核算独立显示，已保存订单与左侧主表单对齐。
      本任务未新增接口、数据库字段或 migration。
- [x] 订单与加卡记录表格操作区整理：订单右侧固定必显“状态/下一步”列并收纳扣减、确认开通，
      通用操作列仅保留详情、修改和更多；加卡记录文字按钮完整显示；专项桌面和移动视口无横向溢出。
      本任务未新增接口、数据库字段或 migration。
- [x] 全局数据表列显示设置：41 张注册表统一提供列设置抽屉，偏好按用户和表标识存入数据库，
      桌面和移动卡片同步生效；支持搜索、全部显示、恢复默认、关闭保护与审计，并通过刷新持久化浏览器验收。
- [x] 礼品卡入账界面重构：合并重复计算信息，改为目标 ID 概览、卡片资料、结算与备注双栏结构，
      保留原有校验、卡商余额预警、确认和关闭保护，并保证窄屏无重叠。
- [x] 新增 ID 采购入账字段整理：统一“ID采购币种、ID采购金额”命名，将人民币成本移动到采购金额下方，
      并明确标记为系统自动计算、无需手动填写。
- [x] 跨页面数据新鲜度与多人并发一致性：设置新增项通过共享失效缓存自动进入订单快捷录入；
      账户余额与候选 ID 接收后台刷新结果；Realtime 异常时每 15 秒版本补偿并在页面回到前台时校验；
      服务端行锁、串行化事务、冲突重试和幂等账本通过专项测试。
- [x] 敏感资料角色策略与管理员审核闭环：管理员直接查看；普通角色可配置直接查看或申请审批；
      申请、批准、拒绝、15 分钟授权和实际查看均写审计，并通过 V2 通知入口提示管理员。
- [x] 单一管理端与 API 入口
- [x] 登录、角色权限与审计
- [x] 本地 JWT 稳定会话、首次强制改密、全设备会话撤销与前端刷新闭环
- [x] ID 与客户资料
- [x] 业务选项设置
- [x] 加卡、移动加权平均成本与不可变余额流水
- [x] 订单录入、自动匹配、锁定、余额扣减与利润
- [x] 订单录入自动/手动合格 ID 选择与预计利润反推收款建议
- [x] 订单查询、修改、退款、取消与软删除
- [x] 开通记录
- [x] 手工续费闭环
- [x] 续费日期筛选、1–365 天预警设置与顶部提醒
- [x] 汇率自动采集、人工记录与设置
- [x] 订单录入右侧快捷新增客户并自动回填
- [x] 订单录入稳定两列布局、目标利润与实时成本核算
- [x] ID 保留/卖出/收回、成本快照、建议价格、订单与 ID 管理闭环
- [x] 已卖出 ID 在匹配、加卡、余额调整和续费入口统一排除
- [x] ID 报损冻结、余额成本保留、损耗入账、解除冻结冲回与 ID 报损记录
- [x] 加卡记录“被赎回”可原子联动报损冻结 ID
- [x] 加卡供应商期初资金、USDT 付款、加卡扣减、撤回返还、调账与付款撤销闭环
- [x] 加卡记录国家/供应商快照、供应商余额快照、付款报表与完整卡号直接展示
- [x] 员工账号开通、角色分配、启停、会话撤销与变更审计
- [x] 角色创建、权限矩阵、角色成员查看、系统角色保护与变更审计
- [x] 审计日志筛选、变更详情、敏感访问查看、敏感字段双层脱敏与导出留痕
- [x] 安全中心登录风险、在线会话下线、MFA 绑定与管理、IP 白名单和防锁死门禁
- [x] 我的账户脱敏资料、修改密码入口、MFA 和当前账号在线设备自助管理
- [x] 权限感知的经营仪表盘：今日业务、风险待办、ID 库存账面成本和审计摘要
- [x] 管理员业务监控：九条源状态规则、服务端分页筛选、异常汇总和源数据处理入口
- [x] 管理员系统监控：API、数据库、版本补偿、汇率任务、认证聚合和不可观测项声明
- [x] 数据治理只读预检 API：软删除盘点、既有导入导出能力、保留清理与备份证据缺口
- [x] 数据治理闭环：回收站恢复、汇率历史受控清理、确定性预览、异人审批、分批检查点和逐项审计
- [x] 数据治理审批就绪门禁：核验其他启用管理员，前后端阻止创建无人可审批的治理预览
- [x] 数据治理待审批任务取消闭环：仅申请人可取消，保留不可变预览并写入事务审计
- [x] 表单左侧标签与必填星号右置规则
- [x] 全站业务表单可提交校验、错误计数、首错定位与硬阻塞原因
- [x] 应用壳、查询缓存、共享骨架、意图预取与 Chunk 恢复
- [x] 区域查询受控阶段、参数切换旧快照只读、稳定分页与详情迟到响应隔离
- [x] 数据变化驱动缓存、私有 Realtime 精确 scope 失效、首次版本基线、版本补偿与时间 deadline
- [x] 桌面端、平板和移动端基础验收
- [x] 全站数据表语义固定列、弹性文本列与容器自适应列间距
- [x] 浅色与暗色主题前景/背景对比度、公共按钮全状态和自动质量门禁
- [x] 管理平台完整骨架：仪表盘、数据中心、监控中心和系统管理入口
- [x] 删除未注册页面、API、脚本、文档和运行入口
- [x] 生产发布预检、Git 提交溯源、主分支保护与最小权限自动巡检
- [x] 多币种财务盈亏闭环：账户、卡商钱包、开支、自动记账、经营分析、资产估值、对账和关账

## 可靠性重构验收

- [x] 金额领域统一为 `Amount4` / `Rate8`，数据库 Decimal 只在 persistence row mapper 出现
- [x] 写命令统一由 `V2CommandTransactionManager` 管理事务、幂等重放、财务过账和同步审计
- [x] ID 报损拆分为 command handler、repository、posting coordinator 和 query service，并复用外层事务
- [x] 会话统一由 `SessionCoordinator` 管理原子凭据、身份代际、跨标签验证、熔断和只读降级
- [x] API 错误统一输出稳定错误码、request ID、重试语义和向后兼容的 `message`
- [x] 生产刷新性能：API 固定到数据库同区、会话活跃时间限频写入、选择项批量读取和分阶段 Server-Timing
- [x] AST 动态盘点的全部真实桌面表迁移到显式 `V2Table + typed schema`，删除隐式表格代理和双轨列算法
- [x] Prisma runtime、Decimal、直接模型访问、直接事务、raw SQL 和 persistence 外 row mapper 纳入静态门禁
- [x] 加载与并发审计：生产初始包体预算纳入总检查；员工修改增加原子版本抢占；IP 白名单防锁死
      校验、版本控制、审计和实时范围更新合并到同一事务锁；新增并发标准自动门禁
- [x] 使用一次性专用测试账号执行登录后全路由性能、窄屏体验和双浏览器并发端到端验收；未使用生产
      管理员密码，也未用静态 fixture 结果替代真实 API 指标。已覆盖 24 个受保护路由、33 张截图、
      页面/API p50/p95/p99、新数据实时可见以及员工和 IP 白名单并发冲突。

本次可靠性重构不新增数据库表、字段或 migration，原有路由、DTO、幂等键和金额字符串响应保持兼容。
表格数量由 `check:v2-table-standard` 每次从模板和 schema 动态计算，不在任务文档中固定数量。
冷启动与已验证会话 503、Decimal adapter、表格多视口稳定性分别由专用 acceptance
命令验收；这些本地结果不代表已执行生产报损、部署或线上迁移。

ID 销售闭环使用增量 migration
`20260729070000_order_account_disposition`，历史订单默认 `retained`，不回算历史利润。
订单保留 ID 购买成本快照，并通过 `appliedAccountCostAmount` 明确本单实际计入值。已完成订单退款按
原实收全额冲回，本单已售 ID 自动恢复可用并冲回 ID 成本。独立“纠正售出”仅用于员工误操作，
不退款、不改变来源订单业务状态，且保留当前余额、余额成本与来源业务锁。

ID 报损先由增量 migration `20260729085000_account_loss_ledger_type` 单独提交流水枚举值，再由
`20260729090000_account_loss` 创建基础约束和报损结构，避免 PostgreSQL 在同一事务中拒绝引用新枚举值。
`20260806100000_account_loss_freeze_reversal` 将报损升级为 active/reversed 生命周期：报损冻结时保留
ID 余额和人民币成本、写入损耗财务日记并关联当前 active loss；解除冻结必须先生成反向财务日记冲回
损耗，再恢复 ID 状态并清除 active loss 指针。已卖出 ID 禁止新增报损，历史普通冻结状态不自动回填
为报损，解除冻结不自动恢复历史开通记录，原报损记录保留为已冲回状态。
礼品卡联动报损复用该表和流水类型；卡片损失与扣卡后剩余 ID 损失分别记账，并使用同一原因和确定性
幂等关联。

生产发布加固任务新增固定 AWS 目标、干净且已同步的 `main`、必需 CI 和分支保护预检；
部署后自动使用 `production_smoke_readonly` 独立账号检查公共健康端点、
最小权限集合和核心只读接口。只同步所需查看权限并新增角色和账号数据，不新增表、字段或 Prisma
migration。

认证闭环复用现有 `V2AuthIdentity.mustResetPassword`、`lastAuthenticatedAt` 和
`ActiveSession.tokenHash`，不新增表、字段或 migration。会话只在登录成功时显式登记，
历史未登记会话不会在请求阶段自动补建。

员工账号和角色权限代码及生产发布已经完成。角色编码创建后保持不可变，`admin` 系统角色只读，当前
不提供角色硬删除或启停。生产已有 `admin`、`yuangong` 和 `production_smoke_readonly` 三个角色；真实
员工仍需按职责逐账号复核本地身份、角色权限和会话撤销。多币种财务 migration、历史回填、业务
期初数据和遗漏开支确认已经完成，生产聚合审计状态为 `completed`。

数据治理闭环使用三个增量 migration：`20260731010000_data_governance_workflow` 建立任务、条目、
审批和检查点结构，`20260731011000_data_governance_updated_at_defaults` 补齐数据库更新时间默认值，
`20260731012000_data_governance_preview_guards` 固化预览哈希、异人审批和执行范围不可变约束。三个
migration 已在本地开发库和生产环境应用。2026-08-12 已使用两个独立生产身份完成专用测试客户的
恢复预览、异人审批、单条执行和同键幂等重放；任务 `GOV-20260812-9dfc8634` 状态为 `succeeded`，
第二次执行返回同一检查点且标记为重放。演练后巡检身份已恢复唯一只读角色并撤销全部临时会话。
治理概况现在按当前申请人返回启用管理员总数、可作为异人审批人的管理员数量和阻塞原因；创建恢复或
清理预览会在业务预检前及创建事务内重复核验，已有任务的同键幂等重放不受后续管理员状态变化影响。
本次审批就绪门禁不新增表、字段、权限或 migration，也不会自动创建生产管理员。
本地隔离验收会从空 PostgreSQL 16 数据库应用当前完整 migration 链，再由两个独立测试管理员完成
恢复预览、禁止自批、异人审批、预览不可变、分批执行和幂等重放；命令为
`npm run acceptance:v2-data-governance`。本地备份恢复演练还需与托管数据库的真实备份链路分开记录。

## 当前待办

- [x] 复核已执行的多币种财务历史回填，并由业务负责人确认期初余额与遗漏开支
- [ ] 按真实生产账号复核当前 24 个真实模块的权限矩阵（当前管理员与只读巡检角色已完成数据库侧
      权限核对；管理员 MFA、真实菜单/写入/敏感字段交互仍待账号持有人验收）
- [ ] 使用真实业务数据完成一次加卡、订单、退款和手工续费闭环
- [ ] 对新增真实员工逐账号复核本地身份、强制改密标记并验证会话撤销
- [x] 在隔离环境完成全量恢复和备份后新数据对比
- [x] 使用两个独立生产身份完成数据治理预览、异人审批、分批执行和幂等重放演练
- [x] 将生产管理端、API、认证和数据库迁移到 AWS EC2/MySQL，并完成只读巡检与权限门禁验收

生产闭环步骤、负责人输入和验收门禁见 `docs/V2_PRODUCTION_CLOSURE.md`。只读聚合审计命令为
`npm run gate:v2-financial-integrity:production`。

当前共有 24 个注册模块，均为真实模块。数据治理恢复接口只覆盖 ID 资料、客户、业务选项和订单的
软删除记录；清理只覆盖超过保留期且未被礼品卡引用的汇率采集历史，通用硬删除保持关闭。产品边界
与数据规则见 `docs/V2_PRODUCT_SCOPE.md` 和 `docs/V2_ARCHITECTURE.md`。

## 每个任务的完成要求

1. 只修改任务直接相关文件。
2. service 提供基础单元测试。
3. 数据库修改使用 Prisma migration。
4. 前端覆盖加载、错误、空状态、权限和移动端。
5. 运行相关 typecheck、test、lint、build 和架构检查。
6. 记录新增接口、表、字段、迁移命令与剩余风险。
